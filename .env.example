# Required: your Anthropic API key. Used by the Netlify function (dev + prod).
ANTHROPIC_API_KEY=sk-ant-...

# Optional: model override. Default is the cheapest current model.
# ANTHROPIC_MODEL=claude-haiku-4-5-20251001  # default — cheapest
# ANTHROPIC_MODEL=claude-sonnet-4-6           # better quality, ~5× cost
# ANTHROPIC_MODEL=claude-opus-4-7             # best quality, ~15× cost

# Supabase — required for server-side cache and the admin analytics dashboard.
# Find these in your Supabase project: Settings → API.
# Use the service_role key (NOT the anon key); it must never reach the browser.
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_SERVICE_KEY=eyJ...

# Admin dashboard token — paste this into /admin to unlock.
# Generate any long random string, e.g. `openssl rand -hex 32`.
ADMIN_TOKEN=changeme-long-random-string

# Cache TTL in days. Older cached breakdowns are ignored and refetched.
# Default 30. Set to 0 to disable the server cache temporarily.
CACHE_TTL_DAYS=30

SUPABASE_URL=https://gzgiszftngaqtrkftkjl.supabase.co
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imd6Z2lzemZ0bmdhcXRya2Z0a2psIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3ODY5NDA1NiwiZXhwIjoyMDk0MjcwMDU2fQ.0aSo1dJx6KTNPdRI1XXhLZJGHt4xdzsnS4M7p6MjhJA
ADMIN_TOKEN=91ae1cc473c570b311bc5b99959c84247f1d862c4774b73930653a349ec10a2c
CACHE_TTL_DAYS=30